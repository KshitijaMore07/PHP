<?php
$num = 153;
$temp = $num;
$sum = 0;

while ($temp > 0) {
    $r = $temp % 10;
    $sum += $r * $r * $r;
    $temp = (int)($temp / 10);
}

if ($sum == $num)
    echo "Armstrong Number";
else
    echo "Not Armstrong";
?>
